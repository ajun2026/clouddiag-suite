# STYLE.md — 设计风格指南

> 本文档描述「云端 AI 远程运维助手」系统的**设计风格 / 功能风格 / UI 界面风格**，
> 供开发者和 AI Agent 在迭代时保持一致，避免每次改版都踩同一个坑。
> 维护原则：**任何一次被用户认可的 UI/交互改动，都要沉淀回本文档**。

---

## 一、系统定位

面向**电脑售后工程师**（技术偏强、非专业开发）的云端 AI 远程诊断平台：

```
浏览器工作台 (Web UI)  ←→  云服务器 (FastAPI + AI 大脑)  ←→  客户机桥接器 (bridge)
     工程师操作             诊断/命令/审批                   .exe / PowerShell 命令版 / Linux
```

核心场景：远程诊断（AI 对话）→ 工具操作（SN/MTM 刷写、OA3 激活、BIOS/驱动/睡眠报告）→ 工单沉淀。

---

## 二、设计哲学（为什么这样做）

| # | 原则 | 含义 | 来源 |
|---|---|---|---|
| 1 | **极简，拒绝冗余** | 不放大横幅、不堆重复快捷入口、不做无意义装饰；一屏只解决一件事 | 用户多次明确反感"冗余 UI 元素""页面堆满了东西" |
| 2 | **主次分明 + 默认折叠** | 高频操作优先露出，次要信息收进折叠/弹窗；页面默认不"摊大饼" | 下载页三步卡片、命令版"查看详细步骤"折叠 |
| 3 | **卡片化组件** | 一套卡片（grid + 名片 + 原位展开）打天下：工具墙、下载方案、首页步骤都是它 | v7 工具墙拍板"未来工具会很多，要考虑到位" |
| 4 | **适配宽屏** | 宽屏要铺开、不挤在中间 1/3；网格用 auto-fit 让内容平均铺满 | 全屏卡片挤左侧问题（auto-fill 空轨道） |
| 5 | **长数据必须收敛** | 列表 >20 行一律「摘要 + 弹窗」，弹窗带搜索/复制/下载 | BIOS 127 项、驱动 229 条两次踩坑 |
| 6 | **数据要真实可验证** | 展示真实字段（专用显存/驱动版本+日期），不堆模糊指标；判断靠实测不靠注释 | 用户"有依据吗"质询文化 |
| 7 | **安全可见** | 只读自动执行；修改/危险操作必须人工审批弹窗；删除/停用类操作防自锁 | Tier 分级 + 审批体系 |

---

## 三、功能风格

### 3.1 双入口：对话诊断 + 工具模式
- **AI 对话**：自然语言描述问题 → AI 大脑规划 → 远程执行命令 → 分析报告；Tier 2/3 触发审批弹窗
- **工具模式**：纯工具无 AI（如换主板刷 SN/MTM），**说明书驱动**是远期方向（每工具一份 JSON 说明书，前端自动生成表单）

### 3.2 工具 = 卡片名片 + 原位展开（accordion）
- 工具墙网格 `auto-fit minmax(320px)`；每卡 = emoji 图标 + 名称 + 徽章（只读蓝/写入红）+ 一句话简介 + 「▶ 打开工具」
- 点击**原位展开**操作区（互斥 accordion），不弹窗、不跳页
- **加新工具 = 丢一张卡片 HTML + fill 函数 + i18n，布局代码零改动**

### 3.3 长列表 → 摘要 + 模态框
- 页面只留摘要行（如"共 229 个驱动"）+ 「查看全部」按钮
- 弹窗模板：`modal-overlay` + `modal-lg-wide` + 搜索框 + 计数 + 复制全部 + 下载 .txt（带头部信息）
- **新工具输出可能 >20 行，直接按弹窗设计，不要先内联等用户喊**

### 3.4 全局三语（zh-CN / zh-TW / en）
- `I18N` 三语字典 + `data-i18n` 属性 + `t()`/`applyLang()`；localStorage key `bridge_ui_lang` 全局共享
- **任何文案改动必须三语同步**；AI 回复语言经 ws 消息 `lang` 字段注入 system prompt
- 校验：新增 key 三语各出现 1 次 + HTML 引用 1 次

### 3.5 数据展示硬规则
- 显卡只显示**专用显存**，不显示总可用内存；需显示驱动版本和日期
- CPU 优先从 dxdiag 取，而非 Systeminfo
- 磁盘匹配 NVMe 的 Model Number 字段
- 判断成功与否永远靠**结构值**（exit_code / license_status 数值），不靠输出文本（GBK 中文不可靠）

### 3.6 安全分级
- Tier 1 只读诊断：自动执行
- Tier 2 交互控制：弹审批（可开"自动放行 Tier2"）
- Tier 3 修改/破坏：必须人工批准（倒计时，超时自动拒绝）
- 危险命令（format/diskpart/reg delete 等）Tier -1 硬拦截，永不执行

---

## 四、UI 界面风格

### 4.1 布局骨架
```
┌──────────────────────────────────────────┐
│ Header：Logo + 系统名 + 用户 + 语言切换    │
├──────────┬───────────────────────────────┤
│ Sidebar  │ Content（flex column 滚动区）  │
│ 200px    │  ├ 首页（默认）                │
│ 操作区    │  ├ 创建/加入房间/对话/下载      │
│ ──────── │  ├ 工单/Windows 工具/Linux 工具│
│ 管理区    │  ├ 修改密码/账号管理/管理后台   │
│ ──────── │  └ 页面 = 标题 + 描述 + 内容   │
│ 设置区    │                               │
└──────────┴───────────────────────────────┘
```
- 侧边栏导航**按「操作区 → 管理区 → 设置区」分组**（分隔线）
- 登录默认进**首页**（先了解系统是干啥的），不是工单列表
- 对话页嵌工作台右侧 iframe（同源，`allow-same-origin allow-downloads`，无 allow-top-navigation）

### 4.2 配色 Token（CSS 变量，深色主题）
| 变量 | 用途 |
|---|---|
| `--bg` | 页面背景（深色） |
| `--surface` / `--surface2` | 卡片/面板 / 次级块（代码块、输入） |
| `--text` / `--text2` | 主文字 / 次要文字 |
| `--border` | 边框 |
| `--info` (#60a5fa) | **主色：亮蓝**——按钮主色、链接、选中态 |
| `--accent` | 玫红——**只保留给发送按钮**，不做弹窗/装饰（用户拍板） |
| `--success` (绿) | 成功、在线、推荐徽章 |
| `--error` (红) | 错误、写入类徽章 |
| `--warn` (橙) | 警告 |

- 主/次按钮：**实心亮蓝 = 主**（深字 #0b1a2e），**透明描边 = 次**；主次靠实心 vs 描边区分
- 徽章：`badge-read` 蓝（只读）/ `badge-write` 红（写入）/ `badge-rec` 绿（推荐）

### 4.3 组件规范
| 组件 | 规范 |
|---|---|
| 卡片 `.panel` / `.tool-card` / `.dl-card` | `surface` 背景、1px 边框、12px 圆角、padding 18-22px、hover 亮蓝描边 + 轻阴影 |
| 网格 | `repeat(auto-fit, minmax(280~320px, 1fr))`——**必须 auto-fit**（auto-fill 会留空轨道，卡片挤左侧） |
| 步骤卡片 `.dl-step` | `surface2` 背景 + 编号圆圈（`.num` 玫红底白字）+ 深色代码块 `.dl-cmd-block` |
| 代码块 `.dl-cmd-block` | 深色底 + 亮蓝字 + `word-break: break-all` + 右侧「复制」按钮 |
| 弹窗 | `modal-overlay` + `modal-lg-wide` + 遮罩点击关闭，`classList.add/remove('show')` |
| 消息时间戳 | **用户消息时间在右侧，AI 回复时间在左侧**（用户明确指定） |
| 复制按钮 | 命令文本必须包 `<span class="dl-cmd-text">`（copyCmd 取 `parentElement.querySelector('span')`，用 code 会复制到按钮文字） |

### 4.4 响应式断点（桌面窗口缩放适配）
```
>1100px   3 列卡片铺满（auto-fit）
≤1100px   内容区 padding 收窄（24px→18px）
≤900px    侧边栏 200→150px，导航字号 12px
≤700px    顶部压缩、隐藏副标题、三步条紧凑；卡片 1 列
```
- 手机/平板**暂不适配**（用户拍板），桌面浏览器缩放平滑即可
- 铁律：flex 子项要有 `min-width: 0` / `min-height: 0`，否则内容撑破容器

### 4.5 宽屏适配
- 主内容 max-width 放宽（对话页 1400px），消息区普通消息 78%、AI 长文本 max-width 920px
- 面板 `.panel` max-width 640px（表单类），卡片网格不设 max-width（网格自己管宽度）

---

## 五、可复用模式清单（给 AI / Agent 快速上手）

### 加一个新工具（工具墙）
1. `tool-grid` 里丢一张 `.tool-card`（icon emoji + 标题 + badge + 一句话 + 打开按钮）
2. 卡片体内放房间下拉 + 操作按钮 + 结果区
3. 写 `fillXxxRoom()`（房间过滤 bridge_online）挂 `initTools()` 末尾
4. i18n 三语加 key
5. 输出可能 >20 行 → 直接弹窗模板，别内联

### 加一种下载方式（下载卡片墙）
1. `.dl-grid` 里丢一张 `.dl-card`（icon + 标题 + 描述 + 操作区）
2. 长步骤收进 `<details class="dl-more">` 折叠
3. i18n 三语加 key

### 三语 i18n 校验
```bash
# 每个新 key：三语字典各 1 次 + HTML data-i18n 1 次
grep -c "key:" static/dashboard.html   # 应 ≥3
grep -c 'data-i18n="key"' static/dashboard.html  # 应 =1
```

### 验证纪律
- JS 语法：**`node --check`**（Python compile 会误报中文注释）
- CSS：括号配平（`{`/`}` 计数为 0 且无负值）
- 页面内容：**必须带登录 cookie curl**（未登录 307 跳登录页会假阴性）
- 版本号：server.py 5 处 + dashboard footer + CHANGELOG + HISTORY 同步

---

## 六、踩坑红线（都是真金白银换来的）

1. **auto-fill ≠ auto-fit**：卡片少的网格必须 auto-fit，否则全屏卡片挤左侧、右侧大片空白
2. **patch 会吞相邻行**：改 CSS 块时 old_string 必须包含完整规则，否则误删关键行（曾删掉 `.content` 的 `display:flex;flex-direction:column` 导致滚动链崩）
3. **中文注释禁止进 .ps1**：PowerShell 5.1 按 GBK 读无 BOM UTF-8，中文变乱码直接解析失败
4. **PowerShell 后台线程（Task.Run）访问不到脚本变量**：后台 scriptblock 无作用域链，$script: 也救不了；要么全主线程同步，要么显式传参
5. **ClientWebSocket 的 ReceiveAsync 取消会杀连接**：想用超时发心跳，取消后连接直接坏（25s 必断）；保活用 `KeepAliveInterval`（协议层 ping）
6. **长列表一定弹窗**：BIOS 127 项、驱动 229 条两次被用户点名"太长了"
7. **复制按钮结构**：`dl-cmd-block` 内命令文本必须是第一个 `<span class="dl-cmd-text">`，否则复制到"复制"两个字
8. **static/*.html 热生效，server.py 必须重启**；改版本号后 `curl /api/health` 验证
9. **i18n 换行转义**：JS 字符串命令换行写 `\n`，patch 时写 `\\n` 会双重转义显示成字面 `\n`

---

## 七、文档索引

| 文档 | 内容 |
|---|---|
| README.md | 项目总览、部署、API 清单 |
| CHANGELOG.md | 版本变更明细（按版本号递增） |
| HISTORY.md | 版本演进史（时间线 + 关键转折） |
| **STYLE.md（本文）** | 设计/功能/UI 风格 + 可复用模式 + 踩坑红线 |
