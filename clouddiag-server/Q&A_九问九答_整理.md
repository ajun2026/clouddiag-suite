# 云端 AI 远程运维助手 — Q&A 与后续规划

> 整理时间：2026-08-02
> 基于当前 MVP 版本（server.py + bridge.py + static/index.html）的讨论

---

## 问题 1：AI Agent 是谁？在哪里？

**AI Agent 就是 `server.py` 里的 Agent 循环代码**，运行在云服务器 `<公网入口IP>` 上。

具体流程：
1. 用户在浏览器发消息 → WebSocket 到 `server.py`
2. `server.py` 的 `run_agent()` 函数将消息 + 工具定义发给 DeepSeek API（`api.deepseek.com`）
3. DeepSeek 返回 `tool_calls`（"我该调用哪个工具"）→ Agent 转发给桥接器
4. 桥接器在 Windows 上执行命令 → 结果回传 → Agent 喂给 DeepSeek
5. 循环直到 DeepSeek 返回最终文本回复 → 推给浏览器

**总结：AI 的"大脑"在 DeepSeek 云端，AI 的"手脚"在你的服务器 + 桥接器上。**

---

## 问题 2：目前 AI Agent 能做什么？

当前有 **4 个工具**，全部为只读诊断：

| 工具 | 能力 | 实现方式 |
|:---|:---|:---|
| `run_systeminfo` | 获取 Windows 系统信息（OS 版本、硬件、内存、网卡等） | `systeminfo` 命令 |
| `run_dxdiag` | DirectX 诊断报告（显卡、声卡、驱动状态） | `dxdiag /t` 命令 |
| `read_event_log` | 读取 Windows 系统事件日志中的 Error/Warning | PowerShell `Get-WinEvent` |
| `run_powershell` | 执行任意只读 PowerShell 诊断命令 | `powershell -Command` |

**当前只做诊断分析，不做修复操作。**

---

## 问题 3：bridge.exe 电脑上的软件问题能查看并解决吗？

| 场景 | 当前能否查看 | 能否解决 |
|:---|:---:|:---:|
| 软件报错 | ✅ 可通过 `read_event_log` 抓 Application 日志 | ❌ |
| 软件卡顿 | ✅ 可通过 PowerShell `Get-Process` 看占用 | ❌ |
| 软件闪退 | ✅ 可查 Windows 错误报告（WER） | ❌ |
| 驱动问题 | ✅ 可通过 `dxdiag` 查看驱动状态 | ❌ |
| 蓝屏分析 | ⚠️ 当前不直接支持 minidump 解析 | ❌ |

当前 Agent 的 system prompt 约束为**只读诊断**。要开放修复能力需要：
1. 增加写入类工具（修改注册表、重启服务、安装软件等）
2. 加入审批机制（高危操作需用户浏览器确认）
3. 这两点在 PRD 文档中已有规划（Phase 2/3）

**集成 WinRemote MCP 后（问题 9），能力将从 4 个工具扩展到 40+ 个，覆盖桌面操作、进程管理、文件管理、注册表、服务、网络等。**

---

## 问题 4：系统是否有日志记录？

**当前状态：几乎没有。**

| 日志类型 | 当前状态 | 详情 |
|:---|:---:|:---|
| bridge.exe 运行日志 | ❌ | 只有控制台输出，窗口关了就没 |
| 用户对话内容 | ❌ | server.py 没有持久化，服务重启全丢 |
| 服务器运行日志 | ⚠️ 部分 | `server.log` 只有 HTTP 请求日志，没有对话内容 |
| 房间连接记录 | ❌ | 只在内存中，不持久化 |

**改善方案（可行性：✅ 完全可行）：**
- `server.py`：对话消息写入 SQLite/PostgreSQL（PRD 中 `messages` 表已设计）
- `bridge.py`：加本地日志文件（`bridge.log`）
- 所有 `print` 改为 `logging` 模块

**预估改动量：中等。**

---

## 问题 5：B 电脑能否看到 A 电脑的诊断记录？

**当前状态：不能。**

浏览器断开后 rooms 和 messages 都在内存中消失。B 电脑输入 A 的房间号后无从得知历史。

**改善方案（可行性：✅ 可行）：**
- 对话消息持久化到数据库
- B 电脑输入房间号后，服务端推送历史消息
- PRD 中「多用户协作」（F14）和「项目记忆」（F15）已涵盖此需求

**预估改动量：中等。**

---

## 问题 6：是否有统一管理后台？

**当前状态：没有。**

**改善方案（可行性：✅ 完全可行）：**

可做一个管理员页面（如 `/admin`）：
- 列出所有房间、桥接器连接状态
- 点击房间查看实时对话或历史记录
- 所有抓取信息统一存数据库
- 对应 PRD Phase 3 的「审计后台 + 用量统计」

**预估改动量：较大。**

---

## 问题 7：页面能否提供 bridge.exe 下载和使用说明？

**可行性：✅ 完全可行，改动极小。**

当前 `server.py` 已 serve `static/` 目录：
- 把 `bridge.exe` 放到 `static/` 目录下
- 首页加一个「下载桥接器」按钮和一个使用说明区域
- 用户打开网页就能下载、看说明

**预估改动量：极小。**

---

## 问题 8：用户能否导出对话内容？

**可行性：✅ 可行。**

分两档实现：
- **简单版**：前端 JS 把对话 DOM 内容拼接成文本/HTML，一键下载（纯前端，不改后端）
- **完整版**：后端生成 PDF（如 WeasyPrint），从数据库拉历史消息渲染导出

PRD 中 F16「报告导出」即此功能。

**预估改动量：小到中等。**

---

## 问题 9：能否集成 WinRemote MCP 的 45 个工具？

**结论：✅ 完全可行，且是绝佳搭配。**

### WinRemote MCP 的 45 个工具（8 大类）：

| 类别 | 工具 | 当前有无 |
|:---|:---|:---:|
| **桌面** | 截图、标注截图、OCR 文字识别、录屏、播放声音 | ❌ |
| **输入** | 鼠标点击、键盘输入、滚动、拖拽、快捷键 | ❌ |
| **窗口管理** | 聚焦窗口、显示桌面、启动/切换应用 | ❌ |
| **系统** | Shell、剪贴板、进程列表、杀进程、系统信息、通知、锁屏 | ⚠️ 部分 |
| **文件系统** | 读文件、写文件、列目录、搜索文件、下载/上传 | ❌ |
| **注册表&服务** | 读/写注册表、列出/启停服务 | ❌ |
| **计划任务** | 列出/创建/删除计划任务 | ❌ |
| **网络** | 抓网页、Ping、端口检测、网络连接、事件日志 | ⚠️ 部分 |

### WinRemote MCP 源码结构（已确认）：

```
src/winremote/
├── __main__.py      2045行  ← 45 个 MCP 工具定义（@mcp.tool() 装饰的 Python 函数）
├── desktop.py        361行  ← 截图、窗口枚举、UI 元素、剪贴板
├── network.py         69行  ← ping、端口检测、网络连接
├── process_mgr.py    107行  ← 进程列表、杀进程
├── ocr.py                   ← OCR 文字识别
├── registry.py              ← 注册表读写
├── services.py              ← 服务管理
├── taskmanager.py           ← 计划任务管理
├── recording.py             ← 屏幕录制
├── auth.py                  ← 认证 & 安全
├── config.py          123行  ← 配置管理
└── tiers.py                 ← Tier 1/2/3 分级
```

### 三种集成方案对比：

| | A. 替换 | B. 并存 | C. 内嵌 ✅ |
|:---|:---:|:---:|:---:|
| 做法 | 扔掉 bridge.py，包装 WinRemote MCP | bridge.py 启动时同时跑 WinRemote MCP | 把 WinRemote 的功能模块复制进 bridge.py |
| 客户只需一个 exe | ❌ | ❌ | ✅ |
| 不需要安装 Python | ✅ | ❌（要 pip install） | ✅ |
| 保持自有 WebSocket 协议 | ❌ 要大改 | ⚠️ 部分 | ✅ |
| 可控制开放哪些工具 | ❌ | ⚠️ | ✅ |
| 后续好维护 | ❌ | ⚠️ | ✅ |
| 工作量 | 1~2 天 | 1~2 天 | 3~5 天 |

### 推荐：方案 C（内嵌）

把 WinRemote MCP 的功能模块直接整合到 `bridge.py`，保持一个 exe、一个 WebSocket 连接的简洁架构，同时获得 40+ 个工具能力。保留 Tier 分级：Tier 1-2 自动执行，Tier 3（写文件、杀进程、改注册表等）需用户在浏览器确认。

---

## 汇总

| # | 问题 | 可行性 | 当前状态 | 预估改动 |
|:---:|:---|:---:|:---|:---:|
| 1 | Agent 在哪 | — | ✅ 已实现 | — |
| 2 | Agent 能做什么 | — | 4 个只读工具 | 可扩展 |
| 3 | 软件问题修复 | — | 只诊断不修复 | 需加修复+审批 |
| 4 | 日志记录 | ✅ | ❌ 没有 | 中等 |
| 5 | 多端查看历史 | ✅ | ❌ 不行 | 中等 |
| 6 | 管理后台 | ✅ | ❌ 没有 | 较大 |
| 7 | 页面提供下载 | ✅ | ❌ 没有 | 极小 |
| 8 | 导出对话 | ✅ | ❌ 没有 | 小~中 |
| 9 | 集成 WinRemote MCP | ✅ | 待实施 | 3~5 天（方案 C） |

---

## 建议实施优先级

| 优先级 | 任务 | 理由 |
|:---:|:---|:---|
| P0 | ~~问题 7：页面提供 bridge.exe 下载 + 使用说明~~ | ✅ 已完成（2026-08-02） |
| P0 | 问题 9：集成 WinRemote MCP（方案 C） | 核心能力跃升，4→40+ 工具 |
| P1 | ~~问题 4：日志记录~~ | ✅ 已完成（2026-08-02） |
| P1 | ~~问题 8：导出对话~~ | ✅ 已完成（2026-08-02） |
| P2 | 问题 5：多端查看历史 | 团队协作场景需要 |
| P2 | 问题 3：修复操作 + 审批 | 从诊断到修复的闭环 |
| P3 | 问题 6：管理后台 | 多用户后必需 |

---

## 已完成任务详情（2026-08-02）

### 问题 7：页面提供 bridge.exe 下载 + 使用说明 ✅

**实现方式：**
- `bridge.exe`（8.8MB）复制到 `static/` 目录
- 登录弹窗（`static/index.html`）底部新增：
  - **使用说明**：4 步操作指引（下载→双击→输入房间码→开始诊断）
  - **注意事项**：说明仅执行只读诊断
  - **下载按钮**：点击即下载 `/static/bridge.exe`

**验证：** `curl -s -o /dev/null -w "%{http_code}" http://<公网入口IP>:8000/static/bridge.exe` → 200

---

### 问题 8：导出对话 ✅

**实现方式：**
- 前端顶部状态栏右侧新增「导出对话」按钮（连接房间后显示）
- 点击后 JS 遍历页面消息 DOM，拼接为 txt 文件下载
- 文件命名：`诊断报告_{房间码}_{日期}.txt`
- 内容包含：用户消息、AI 回复、工具执行结果、系统消息
- 纯前端实现，不依赖后端

---

### 问题 4：日志记录 ✅

**server.py 改动：**
- 引入 `logging` 模块，`print` 全部改为 `log.info()`/`log.error()`
- 两类日志文件（均在 `logs/` 目录）：
  - `logs/server.log`：运行日志（房间创建、桥接器/浏览器连接/断开）
  - `logs/chat.log`：对话日志（用户消息、AI 回复，按房间码标注）
- 日志带时间戳，UTF-8 编码

**bridge.py 改动：**
- 引入 `logging` 模块，`print` 全部改为 `log.info()`/`log.error()`
- 日志写入 `logs/bridge.log`（bridge.exe 同级目录）
- 记录内容：连接信息、工具调用、执行耗时、错误、断开
- 支持命令行参数：`bridge.exe [服务器地址] [房间码]`
- 已重新打包 `dist/bridge.exe`

**查看日志：**
```bash
# 服务器端（SSH登录）
ssh ubuntu@<公网入口IP>
tail -f /home/ubuntu/cab-server/logs/chat.log      # 对话内容
tail -f /home/ubuntu/cab-server/logs/server.log    # 连接记录

# 桥接器端（Windows）
notepad logs\bridge.log   # 在 bridge.exe 同级目录
```

---

*本文档由 AI 助手整理，持续更新中。*
*最后更新：2026-08-02 12:30*
