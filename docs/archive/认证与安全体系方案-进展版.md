# 认证与安全体系方案（实施进展版）

| 项目 | 内容 |
|:---|:---|
| 版本 | v1.1（v1.0 + 完成状态标注） |
| 日期 | 2026-08-11 |
| 状态 | 部分实施中 |
| 图例 | ✅ 已完成 · ⏳ 待实施 |

---

## 一、背景与现状风险

| 现状（v1.0 时） | 风险 | 状态 |
|---|---|---|
| 通用 exe + 8 位房间码即可连接 | 房间码可被分享/猜测，弱凭证 | ⏳ 待实施（连接令牌） |
| 无"离开失效"机制 | 工程师离开后客户机上的 exe 仍有效 | ⏳ 待实施（结束诊断） |
| 登录有效期 12h，无空闲超时 | 忘关页面 = 身份长期有效 | ⏳ 待实施 |
| 无登录日志 | 账号泄漏无法及时发现/审计 | ⏳ 待实施 |
| **HTTP/WS 明文传输** | **凭证/命令可被中间人窃听（最大缺口）** | ✅ **已解决（HTTPS/WSS）** |

---

## 二、连接令牌（桥接器唯一化）

**凭证从"房间码"升级为"房间码 + 连接令牌"**：

```
创建房间 → 服务器生成 connect_token（32 位强随机，绑定房间）
    ↓
一键连接（命令/.bat/Linux）自动内嵌令牌
    ↓
bridge 连接 → 提交 房间码 + 令牌 → 服务器校验（匹配 + 未失效）
    ↓
通过才放行；无令牌 / 令牌错 / 已作废 → 拒绝
```

| 要点 | 状态 |
|---|---|
| 服务器只存令牌哈希（防 DB 泄漏） | ⏳ 待实施 |
| 管理员旁路（接管/审计） | ⏳ 待实施 |
| Go bridge 支持 `-token` 参数 | ⏳ 待实施 |
| ps1 支持 `$env:BRIDGE_TOKEN` | ⏳ 待实施 |
| 传统手动输房间码废弃 | ⏳ 待实施 |

---

## 三、失效机制（三重保险）

| 失效方式 | 触发 | 状态 |
|---|---|---|
| **结束诊断（一键关闭）** | 对话页/工单点「⛔ 结束诊断」：令牌作废 + 网页不可访问 + 历史封存 | ⏳ 待实施 |
| 房间有效期到期 | expires_at（**已实现** v0.11.0） | ✅ 房间部分已完成，令牌联动待实施 |
| 令牌有效期 | 生成后限时（如 24h） | ⏳ 待实施 |

---

## 四、机器特征绑定（跨平台）

| 平台 | 唯一标识 | 状态 |
|---|---|---|
| Windows | MachineGuid（注册表） | ⏳ 待实施 |
| Linux | /etc/machine-id（systemd） | ⏳ 待实施 |
| 兜底 | DBus machine-id、主板 UUID | ⏳ 待实施 |

- 桥接器统一收集 `machine_id` 字段（平台无关）
- ❌ 不使用：MAC / hostname / MTM
- 可选启用（默认关闭）

---

## 五、有效期体系（三个维度，独立设计）

| 维度 | 对象 | 建议量级 | 状态 |
|---|---|---|---|
| **账号登录会话** | 工程师账号 | 4h 绝对 + 30min 空闲登出 | ⏳ 待实施（当前仍 12h） |
| **连接令牌** | 桥接器授权 | 24h | ⏳ 待实施 |
| **房间有效期** | 工单业务 | 7/15/30 天/永久 | ✅ **已完成**（v0.11.0） |

---

## 六、防资源滥用（账号泄漏防护）

| 层 | 机制 | 状态 |
|---|---|---|
| 资源 | 每账号并发房间上限（2）+ 日诊断次数上限（20） | ⏳ 待实施 |
| 入口 | 信任设备/新设备验证（二期可加 2FA） | ⏳ 待实施 |
| 监控 | 登录日志 + 异常告警 | ⏳ 待实施 |
| 止损 | 管理员一键停用账号 | ⏳ 待实施 |

---

## 七、登录审计与账号管理

| 项 | 状态 |
|---|---|
| 登录日志表（工号/IP/设备/结果） | ⏳ 待实施 |
| 异常标记（非联想网络/新设备/多地同时/防爆破） | ⏳ 待实施 |
| 口令策略（强度/锁定） | ⏳ 待实施 |
| 账号生命周期（离职停用/强制下线） | ⏳ 待实施 |

> 注：服务器已支持 X-Forwarded-For（v0.11.2）——反代后真实客户端 IP 可记录，为登录日志实施铺路 ✅

---

## 八、传输安全（HTTPS/WSS）—— ✅ 已完成

### 落地成果（v0.11.2，2026-08-11）

| 项 | 完成情况 |
|---|---|
| **域名** | ✅ clouddiag.online（腾讯云，A 记录 → <服务器IP>） |
| **反向代理** | ✅ Caddy（80/443）→ 127.0.0.1:8000 |
| **证书** | ✅ Let's Encrypt 免费证书（90 天自动续期） |
| **HTTP 跳转** | ✅ http://clouddiag.online → 308 → https |
| **旧地址缓冲器** | ✅ http://<服务器IP>（80 端口）→ 提示页引导新地址 |
| **IP 直连拦截** | ✅ server.py Host 校验中间件（IP:8000 只显示提示页，无法使用；Host 从 PUBLIC_URL 动态推导，多部署兼容） |
| **ps1 wss** | ✅ v1.2.0（手写 WS 层 TLS 1.2 握手 + 证书校验） |
| **Go bridge wss** | ✅ 默认 wss://clouddiag.online，四架构重新编译 |
| **bridge.py** | ✅ SERVER_URL → wss://clouddiag.online |
| **一键连接** | ✅ 三端 https/wss；.bat 带 `-server {{WS_URL}}`（多部署兼容） |
| **X-Forwarded-For** | ✅ uvicorn proxy_headers + ws_bridge 读取（真实客户端 IP 记录） |
| **8000 公网关闭** | ⏳ 待主人操作（腾讯云安全组删 8000，已设 8/12 10:00 提醒） |

### 遗留

- 安全组关闭 8000 端口（应用层已拦，端口级封锁待操作）

---

## 九、实施优先级（三批）

| 批次 | 内容 | 状态 |
|---|---|---|
| **第一批** | HTTPS/WSS 传输加密 | ✅ **已完成** |
| **第一批** | 连接令牌 + 结束诊断一键关闭 | ⏳ 待实施 |
| **第一批** | 登录有效期 4h + 空闲 30min 登出 | ⏳ 待实施 |
| **第二批** | 登录日志 + 资源限额（并发 2 + 日 20） | ⏳ 待实施 |
| **第二批** | 机器特征绑定（machine_id） | ⏳ 待实施 |
| **第三批** | 信任设备 / 2FA / 口令强化 / 导出管控 / 限流 | ⏳ 待实施 |

---

## 十、设计权衡

| 项 | 权衡 |
|---|---|
| 令牌 + 结束诊断 | 工程师多一步操作；换来离开即失效 |
| 登录 4h + 空闲 30min | 体验 vs 安全——活跃续期保证干活不打断 |
| 资源限额 | 正常工程师够用；泄漏者消耗封顶 |
| 管理员旁路 | 审计/救场必需；管理员账号用日志+告警重点盯防 |
| 机器绑定（默认关） | 更严但增加换机成本；按需启用 |
| HTTPS 已落地 | 全站加密 ✅——后续安全项在此基础上叠加 |
