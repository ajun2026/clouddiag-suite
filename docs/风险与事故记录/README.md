# 风险与事故记录

> 这里记录本项目在开发、部署、运维中**真实踩过的坑**。
> 目的：提醒自己，也提醒每一个部署和使用本项目的人。
>
> **建议在部署前先浏览一遍**——多数问题只要提前知道，就能避开。

---

## 索引

### A 类 · 部署相关

| 编号 | 问题 | 影响 | 状态 |
|---|---|---|---|
| [A1](A1-Linux桥接器与系统命令同名.md) | Linux 桥接器与系统网络工具同名（都叫 `bridge`） | 客户误执行时被劫持终端 | ✅ 已改名修复 |
| [A2](A2-依赖缺失导致功能异常.md) | Python 依赖 / 系统工具缺失导致启动或功能失败 | 服务起不来 / 上传失败 | ✅ 已补全清单 |
| [A3](A3-端口硬编码导致部署不一致.md) | 端口写死在代码里，不同环境部署需改代码 | 部署不一致、易冲突 | ✅ 已改为可配置 |

### B 类 · 开发相关

| 编号 | 问题 | 影响 | 状态 |
|---|---|---|---|
| [B1](B1-桥接器实现语言误判.md) | 误以为桥接器是 Python（实为 Go），改错文件 | 浪费时间、走弯路 | ✅ 已澄清 |
| [B2](B2-提示词多处消费路径未同步.md) | 同一份提示词存在多个消费路径，改一处漏两处 | 部分功能行为不一致 | ✅ 已建立同步清单 |
| [B3](B3-包内混入旧文件.md) | 发布包中混入比现网更旧的文件 | 部署后 bug 复发 | ⚠️ 需按文件比对升级 |

### C 类 · 安全相关

| 编号 | 问题 | 影响 | 状态 |
|---|---|---|---|
| [C1](C1-环境文件误提交公开仓库.md) | `.env`（含真实凭据）被提交到公开仓库 | **凭据泄露** | ⚠️ 文件已移除，凭据需轮换 |

### D 类 · 运维相关

| 编号 | 问题 | 影响 | 状态 |
|---|---|---|---|
| [D1](D1-服务进程被连带杀死.md) | 服务未脱离父进程组，网关重启时被连带杀掉 | 服务意外中断 | ✅ 已用 systemd 解决 |

---

## 通用预防清单

部署/升级本项目时，建议逐项确认：

- [ ] 系统依赖齐全（`7z` / `unrar` / `lzop` / Python 包）
- [ ] `.env` 已按部署形态配置（特别是 `COOKIE_SECURE`）
- [ ] `PUBLIC_URL` 与实际访问地址一致
- [ ] 端口规划无冲突（server / log-analyzer）
- [ ] 桥接器协议与部署形态匹配（HTTP→`ws://`，HTTPS→`wss://`）
- [ ] 升级时按文件比对，不整体覆盖（避免旧文件覆盖现网修复）
- [ ] `.env` 等含密钥的文件**不进入版本库**
- [ ] 部署后跑 `deploy/doctor.sh` 自检
- [ ] 升级前备份（含运行时数据）

---

## 记录格式约定

每篇记录结构统一：

```
现象 → 根因 → 影响范围 → 处置过程 → 预防措施 → 相关提交
```
